# KaoyanVocab 使用说明

> 考研英语词汇自动化收集系统：做真题时遇到生词 → 一键收藏 → AI 自动补全释义、例句、派生词 → 背单词 / 每日复习 → 周末导出 PDF 打印背诵。

**设计初衷**：市面上的背单词软件大多只能收藏单个单词，词组要么不能收藏、要么译文质量差。KaoyanVocab 以**词组收藏**为核心能力——词组整体收录、AI 生成整句释义、智能词形还原（`took it for granted` → `take it for granted`），真题里遇到的每一个搭配都能一键收藏。

---

## 目录

1. [安装与配置](#1-安装与配置)
2. [录入单词](#2-录入单词)
3. [翻译来源切换](#3-翻译来源切换)
4. [词表管理](#4-词表管理)
5. [背单词](#5-背单词)
6. [每日复习](#6-每日复习)
7. [书签](#7-书签)
8. [数据备份与恢复](#8-数据备份与恢复)
9. [PDF 导出打印](#9-pdf-导出打印)
10. [常见问题](#10-常见问题)
11. [项目结构](#11-项目结构)

---

## 1. 安装与配置

### 1.1 环境要求

- Python 3.9+（已安装 `fastapi`、`uvicorn`、`requests`、`python-dotenv` 可跳过 1.2 的自动安装）
- 一个 AI 服务的 API Key（DeepSeek 必填，小米 MiMo 可选）
- 浏览器（推荐 Chrome，配合右键划词扩展；Safari/Edge 也可直接使用网页）

### 1.2 一键启动

**macOS：**

```bash
bash restart.sh
```

脚本会自动：杀掉占用 8000 端口的旧进程 → 后台启动服务（日志写入 `server.log`，超过 5MB 自动轮转）→ 打开浏览器。

**Windows 11：**

直接双击 **`start.bat`**。脚本会自动完成：

1. 检查 Python 3（未安装会提示，自动过滤微软商店的 python 占位程序）
2. 缺少依赖时自动 `pip install -r requirements.txt`
3. 杀掉占用 8000 端口的旧服务
4. 后台启动服务（日志写入 `server.log`）
5. 打开浏览器访问 `http://127.0.0.1:8000`

> 手动启动方式（两个系统通用）：先 `pip install -r requirements.txt`，再 `python main.py`，然后浏览器打开 `http://127.0.0.1:8000`。

### 1.3 配置 API Key

首次使用前，把项目里的 **`.env.example`** 复制为 **`.env`** 并填入密钥：

```
DEEPSEEK_API_KEY=sk-你的DeepSeek密钥     ← 必填
DEEPSEEK_BASE_URL=https://api.deepseek.com   ← 可选，默认此地址

MIMO_API_KEY=sk-你的MiMo密钥             ← 可选（小米 MiMo）
MIMO_BASE_URL=https://api.xiaomimimo.com    ← 可选
MIMO_MODEL=mimo-v2.5                        ← 可选
```

> `.env` 已被 gitignore，不会上传到 GitHub，也不会泄露。

---

## 2. 录入单词

### 2.1 基本录入（推荐：自动拼写检查）

在顶部输入框**粘贴单词**（如 `receive`），按 **回车**：

1. AI 自动检查拼写
2. 自动做**词形还原**（如 `running` → `run`，`went` → `go`）
3. 自动补全词性、释义、中英文例句、派生词
4. 存入数据库，出现在表格最上方

**拼写纠错**：如果拼写有误（如 `recieve`），会弹出建议窗口，列出候选词。点击建议使用正确拼写，或点「使用原词」保留原样。

### 2.2 录入词组（跳过拼写检查）

粘贴**词组或短语**（如 `take it for granted`），直接点 **「收藏」按钮**。系统会保留词组原形，只做外部时态/单复数归一化（如 `took it for granted` → `take it for granted`）。

> 词组不会触发拼写检查，适合整句短语或你确认无误的单词。

### 2.3 Chrome 浏览器右键划词（扩展）

1. 打开 `chrome://extensions` → 右上角开启「开发者模式」
2. 点「加载已解压的扩展程序」→ 选择项目里的 **`browser-extension/`** 文件夹
3. 在任意网页（真题 PDF、阅读文章等）**选中英文单词 → 右键 → 收集单词**
4. 页面右下角会弹出提示：⏳ 提交中 → ✓ 已收藏 / ✕ 失败

扩展自动跟随网页右上角选择的翻译来源，并实时显示当前模型名。

### 2.4 自动刷新

页面每秒钟检测一次是否有新词（例如通过扩展收藏的）。一旦发现新词，表格自动刷新，无需手动操作。

---

## 3. 翻译来源切换

页面左上角的下拉框可以切换三个翻译来源：

| 选项 | 特点 |
|------|------|
| **DeepSeek** | 默认，速度快，质量稳定 |
| **MiMo V2.5 快速** | 不启用深度思考，约 2-3 秒返回 |
| **MiMo V2.5 深度思考** | 启用深度思考，质量更高但更慢 |

- 选择会保存在浏览器 `localStorage`，下次打开自动恢复
- 同时会同步到后端（`provider_state.json`），浏览器扩展的右键划词自动跟随
- 录入词条和拼写检查都使用当前选择的来源

---

## 4. 词表管理

### 4.1 浏览与排序

表格按**录入时间倒序**排列（最新的在最上面）。每一行显示：词条（含发音按钮 🔊）、词性、释义、语境例句（中英对照）、派生词、录入时间、操作按钮。

点击 🔊 可播放发音（系统英文语音）。

### 4.2 搜索

顶部搜索框支持**中英文模糊匹配**（同时匹配单词和释义），实时显示匹配数量。输入即过滤，清除即恢复。

### 4.3 行内编辑（双击）

**双击任意单元格**即可直接编辑：

- 单词、词性、释义、例句、派生词都可以改
- 按 **Enter** 保存，按 **Esc** 取消
- 派生词按 JSON 格式编辑，例如：`[{"word":"run","pos":"v.","meaning":"跑"}]`

### 4.4 删除

点击行尾的 **✕** 按钮删除该词条（不可恢复，建议定期导出备份）。

### 4.5 重点标记 ⭐

- 点击行尾的 **☆** 按钮，把单词标记为「重点」：行背景变黄，星星变实心 ★
- 再点一次取消
- 工具栏「⭐ 重点词」可切换到只看重点词列表；再点「📋 全部词汇」回到完整列表
- 重点状态会**保留在 PDF 打印结果**中
- 在背单词中标记「不认识」的单词会自动变成重点

### 4.6 隐藏释义（自测模式）

工具栏「**隐藏释义**」按钮：

- 点击后释义列内容被模糊处理，用于自测回忆
- **鼠标悬停**在某个释义上可临时查看
- **点击**某个释义可永久取消该格的模糊
- 再点一次按钮恢复全部显示

---

## 5. 背单词

### 5.1 开始

点击工具栏「**📝 背单词**」，选择本次背诵数量：**50 / 100 / 150 / 200 / 全部**。

- 单词按词表顺序（时间倒序）依次出现，**不是随机**的
- 如果设置了书签，会显示 **「📌 从书签『单词』继续」** 提示和剩余数量，数量按钮从书签位置开始算；不想续背可点「**↺ 从头开始**」

### 5.2 键盘操作

| 按键 | 功能 |
|------|------|
| **Q** | 认识（quality 5，下次复习间隔更长） |
| **E** | 不认识（quality 1，自动标记为重点词） |
| **空格** | 翻到下一张 |
| **R** | 返回上一张 |
| **Esc** | 退出 |
| **点击卡片** | 同「认识」并前进 |

每张卡片先显示单词（可点 🔊 发音），按 Q/E 后显示词性、释义、例句和派生词，并显示该词的下次复习安排（如「下次复习: 3天后」）。

- 深色模式：右上角 🌓 按钮切换
- 底部进度条实时显示背诵进度和「不认识」数量
- 一轮背完后显示 🎉 总结页：按 **空格** 再来一轮，按 **Esc** 退出

### 5.3 书签续背（重点功能）

退出背单词时，系统**自动把书签保存到最后一个背到的单词的下一个位置**。下次打开背单词会自动定位到书签处继续，形成「背一批 → 书签自动前进 → 下次接着背」的循环。

---

## 6. 每日复习

### 6.1 机制说明

基于 **SM-2 间隔重复算法**：

- 每天自动分配复习任务（新词 + 到期复习词），目标数量 100–300，根据最近 7 天的正确率和复习量自适应调整
- **重点词强制优先**进入当天任务
- 逾期未复习的单词按优先级排序（重点、逾期天数、熟练度等综合打分）
- 按钮上的角标显示当天待复习数量

### 6.2 操作

点击「**📅 每日复习**」：

- 打开时提示当天任务构成：`今日 N 词 · 新词 X · 复习 Y`
- 键盘操作与背单词基本一致：**Q** 认识 / **E** 忘记 / **空格** 确认并下一张 / **R** 返回 / **Esc** 退出
- 每次作答后显示该词的下次复习间隔（如「下次复习: 明天 / 3天后 / 7天后」）
- 全部完成后显示今日正确率总结

---

## 7. 书签

书签是一个全局标记（存在浏览器 `localStorage`，key 为 `vocab_bookmark`），用来标记「背到哪里了」。

| 操作 | 方法 |
|------|------|
| **手动设置** | 点击任意行尾的 📌 按钮；再点一次取消 |
| **自动保存** | 使用背单词功能退出时，自动保存到最后一个背到的单词的下一个位置 |
| **跳转** | 工具栏「📌 跳转至书签」按钮，平滑滚动到书签行并闪烁高亮 |
| **查看** | 书签行有左侧蓝色边框，📌 按钮上显示设置时间 |

> 书签是唯一的：设置新书签会自动移除旧书签。书签对应单词被删除后，会提示并从头开始。

---

## 8. 数据备份与恢复

所有数据保存在本地 **`vocab_data.db`**（SQLite 单文件）。

| 操作 | 方法 |
|------|------|
| **备份** | 工具栏「⬇ 导出数据」→ 下载 JSON 备份文件 |
| **恢复** | 工具栏「⬆ 导入」→ 选择之前的 JSON 备份（自动跳过重复词条） |
| **完全重置** | 停止服务 → 删除 `vocab_data.db` → 重新启动（注意：会清空全部数据） |

> 换电脑 / 重装系统：导出 JSON → 新环境启动后导入即可。数据库文件也可以直接拷贝（需先停止服务）。

---

## 9. PDF 导出打印

1. 点工具栏「**导出 PDF**」（或浏览器 Cmd+P / Ctrl+P）
2. 选择「另存为 PDF」
3. 打印排版已适配 A4：包含页眉（词条数、导出时间）、页脚页码，重点词保留黄色标记，工具栏等界面元素自动隐藏

周末打印出来，边走边背，配合书签续背使用效果最佳。

---

## 10. 常见问题

**Q1：打开页面提示「无法连接到后端服务」？**
后端没有启动。运行 `bash restart.sh`（macOS）或双击 `start.bat`（Windows），确认 `http://127.0.0.1:8000` 能打开。

**Q2：录入单词没有反应或报错？**
- 检查 `.env` 是否已配置 API Key（参考 [1.3 配置 API Key](#13-配置-api-key)）
- 查看 `server.log` 尾部日志，常见为网络无法访问 API 服务
- 确认当前翻译来源可用（可切换 DeepSeek / MiMo 试试）

**Q3：端口 8000 被占用？**
启动脚本会自动杀掉占用 8000 端口的进程。若手动启动，请先释放端口。

**Q4：Chrome 扩展提示「无法连接到后端」？**
确认后端已在运行，且浏览器扩展的翻译来源与后端一致（后端重启后会自动同步）。

**Q5：想清空全部数据重新开始？**
停止服务 → 删除 `vocab_data.db` → 重新启动。

**Q6：数据会同步到云端吗？**
不会。所有词汇数据只保存在本机 SQLite；只有调用 AI 补全词条时，才会把单词内容发送给所选 API 服务商（DeepSeek / MiMo）。

**Q7：背单词总是从头开始？**
开了书签会自动从书签继续；若没有书签或点了「从头开始」，则从第一个单词开始。书签位置在退出背单词时自动保存。

**Q8：如何更新项目？**
在项目目录执行 `git pull`，重启服务即可（macOS：`bash restart.sh`；Windows：双击 `start.bat`）。

---

## 11. 项目结构

```
├── main.py               # FastAPI 后端（API + 托管前端 + SQLite）
├── index.html            # 纯 HTML/JS/CSS 前端（无框架、无构建）
├── browser-extension/    # Chrome 右键划词扩展
├── restart.sh            # macOS 一键启动脚本
├── start.bat             # Windows 11 启动入口（双击运行）
├── start-server.ps1      # Windows 11 启动逻辑（PowerShell）
├── .env.example          # API 配置模板（复制为 .env 使用）
├── requirements.txt      # Python 依赖
└── vocab_data.db         # 本地词汇数据库（运行时生成，勿提交）
```

---

*建议工作流：做题收集 → 每日复习巩固 → 背单词推进新词（书签续背）→ 周末导出 PDF 打印。祝考研顺利！🎓*
